import 'package:flutter/material.dart';
class PrimesPage extends StatelessWidget {
  const PrimesPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('P r i m e s — en cours'));
}
