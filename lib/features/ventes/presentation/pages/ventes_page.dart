import 'package:flutter/material.dart';
class VentesPage extends StatelessWidget {
  const VentesPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('V e n t e s — en cours'));
}
